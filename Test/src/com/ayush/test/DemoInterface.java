package com.ayush.test;

public interface DemoInterface {
	
	 int x=12;     //public static and final by default whether we declare it or not
	 //initialization also required
	 void method();//public and abstract by default whether we declare it or not
	 
		/* no instance block
		 * { this is instance block }
		 */
	 
	 /* no static block
		 * static { this is static block }
		 */

}
